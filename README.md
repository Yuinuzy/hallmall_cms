## CMS Halmall Laravel 12
Install all the dependencies using composer

    composer install
Install all the dependencies using npm
    
    npm install

And run the initial migrations and seeders.

    php artisan migrate --seed